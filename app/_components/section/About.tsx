import React from "react";
import SectionHeader from "../common/SectionHeader";

const AboutSection = () => {
  return (
    <div className="">
      <SectionHeader>About</SectionHeader>
      <p>
        A mathematics student and software developer specializing in full-stack
        web applications using React, Node.js, and database technologies.
        Currently expanding into Java, PHP, Laravel, and Spring Boot to build
        scalable applications.
      </p>
    </div>
  );
};

export default AboutSection;
